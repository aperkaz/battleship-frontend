
export const cellCodes = {
    missed: -1,
    default: 0,
    hit: 1,
    boats: [2,3,4,5]
};

export const getOpponent = player => player === 0? 1: 0;

// TODO -check if the board is valid
export const validBoard = board => {

};

const validateShipPositions = (board, ships) => {

};

const validateShipNumber = (board, ships) =>  {

};
